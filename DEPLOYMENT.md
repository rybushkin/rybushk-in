# Инструкции по деплою на GitHub

## ✅ Что уже сделано

1. ✅ Создана папка `_git` с публичными файлами
2. ✅ Исправлены пути к зависимостям
3. ✅ Созданы `.gitignore` и `README.md`
4. ✅ Инициализирован Git репозиторий
5. ✅ Создан первый коммит
6. ✅ Ветка переименована в `main`

## 📋 Следующие шаги

### 1. Создать репозиторий на GitHub

1. Перейдите на https://github.com/new
2. Название репозитория: `rybushk-in`
3. Описание (опционально): "Personal website in CLI style with ASCII/Terminal design"
4. Выберите **Public** (обязательно для GitHub Pages)
5. **НЕ** инициализируйте с README, .gitignore или лицензией (у нас уже есть)
6. Нажмите "Create repository"

### 2. Подключить локальный репозиторий к GitHub

Выполните в терминале:

```bash
cd "/Users/andrey/Library/Mobile Documents/com~apple~CloudDocs/folder/01 - projects/experiments/rybushk-in/code/_git"

git remote add origin https://github.com/rybushkin/rybushk-in.git
git push -u origin main
```

Если репозиторий уже существует и вы хотите перезаписать:

```bash
git remote add origin https://github.com/rybushkin/rybushk-in.git
git push -u origin main --force
```

### 3. Настроить GitHub Pages

1. Перейдите в Settings → Pages
2. Source: **Deploy from a branch**
3. Branch: **main**
4. Folder: **/** (root)
5. Нажмите **Save**

### 4. Проверить сайт

Через несколько минут сайт будет доступен по адресу:
**https://rybushkin.github.io/rybushk-in/**

## 🔄 Обновление проекта

Когда нужно обновить сайт:

```bash
cd "/Users/andrey/Library/Mobile Documents/com~apple~CloudDocs/folder/01 - projects/experiments/rybushk-in/code/_git"

# Скопировать изменения из рабочей версии
cp -r ../rybushk.in\ -\ v0002/* .
cp ../rybushk.in\ -\ v0002\ -\ Claude/styles-ascii.css .

# Удалить приватные файлы
rm -rf Content/ todo.md all-texts.md index-old.html test-doom.html

# Убедиться, что путь к стилям правильный в index.html
# (должно быть: href="styles-ascii.css")

# Закоммитить и запушить
git add .
git commit -m "feat: описание изменений"
git push
```

## 🌐 Настройка кастомного домена (опционально)

Если хотите использовать свой домен (например, `rybushk.in`):

1. Создайте файл `CNAME` в корне репозитория:
   ```
   rybushk.in
   ```

2. Настройте DNS записи у регистратора домена:
   - Тип: **A**
   - Имя: **@** (или оставить пустым)
   - Значение: `185.199.108.153`
   - Добавьте еще 3 A записи с IP:
     - `185.199.109.153`
     - `185.199.110.153`
     - `185.199.111.153`

3. Или используйте CNAME для поддомена:
   - Тип: **CNAME**
   - Имя: **www**
   - Значение: `rybushkin.github.io`

4. Включите "Enforce HTTPS" в Settings → Pages после подтверждения домена

## ⚠️ Важно

- Все приватные файлы (notes, attachments, presentations) остаются в рабочей версии
- В `_git` только публичные файлы для сайта
- При обновлении всегда проверяйте, что не копируются приватные данные

